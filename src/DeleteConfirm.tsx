import { AlertTriangle, Trash2, X } from "lucide-react";
export default function DeleteConfirm({name,kind,busy,error,onCancel,onConfirm}:{name:string;kind:string;busy:boolean;error:string;onCancel:()=>void;onConfirm:()=>void}){return <div className="delete-backdrop" onMouseDown={event=>{if(event.target===event.currentTarget&&!busy)onCancel()}}><section className="delete-dialog" role="alertdialog" aria-modal="true"><header><span><AlertTriangle/></span><button onClick={onCancel} disabled={busy} aria-label="Close"><X/></button></header><h2>Delete this {kind}?</h2><p><strong>{name}</strong> will be removed permanently. This action cannot be undone.</p>{error&&<div className="delete-error"><AlertTriangle/>{error}</div>}<footer><button className="setup-back" onClick={onCancel} disabled={busy}>Keep {kind}</button><button className="delete-confirm" onClick={onConfirm} disabled={busy}><Trash2/>{busy?"Deleting":"Delete permanently"}</button></footer></section></div>}
